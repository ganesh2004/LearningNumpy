import numpy as np
a = np.array([1,2,3])
print(a)
# adding a number to array (adds to each element) same for subtraction,multiplication and division
a += 2
print(a)
# you can also add arrays to each other provide they are of same shape(assuming) can also do arr**2
# squaring each element
# taking sin() of an array
sinofa = np.sin(a)
print(sinofa)
# https://numpy.org/doc/stable/reference/routines.math.html (this has more functions)
